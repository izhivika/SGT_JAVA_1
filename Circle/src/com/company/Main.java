package com.company;

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
/*        // Write a program to calculate the circumference of a circle.
        // The formula for the circumference of a circle is 2 * PI * r.

        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter the radius of a circle: ");
        float pi = (float) Math.PI;
        float radius = scanner.nextFloat();
        System.out.println("The circumference of the circle is " + 2 * pi * radius);

        // Classwork
        // Write an application that calculates the body mass index of a person.
        // Two values will be collected from the user:
        // 1. weight in kg (double)
        // 2. height in m (double)
        // The formula for BMI is weight/height^2
        // If the resulting BMI is not between the range of 18.5 - 24.9 then the user should see the output
        // "BMI not optimal" else "BMI optimal"

        System.out.print("Enter the weight: ");
        double weight = scanner.nextDouble();
        System.out.print("Enter the height: ");
        double height = scanner.nextDouble();
        scanner.close();

        double bmi = weight / (height * height);
        if (bmi >= 18.5 && bmi <= 24.9) {
            System.out.println("BMI optimal");
        } else {
            System.out.println("BMI not optimal");
        }

        // An algorithm is a set of steps or instructions
        // that your program follows to achieve a desired
        // result or solve a problem.
        // 1. Identify the problem.
        // 2. Identify the constraints that need to be satisfied,
        // for example: weight and height.
        // 3. Identify the input for the algorithm.
        // 4. Identify the output for the algorithm.
        // 5. Come up with a solution for the problem.
        // 6. Implement the algorithm.
*/

        // Write a program that outputs numbers from 1 to 50
        // and if the number is divisible by 3, replace it by "Fizz",
        // and if the number is divisible by 7, replace it by "Buzz",
        // and if the number is divisible by both 3 and 7, then replace it with "FizzBuzz".
    }
}